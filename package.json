{
  "name": "mathexam",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "serve": "vue-cli-service serve",
    "start:backend": "cd backend &&nodemon --env-file=.env server.js",
    "start:frontend": "cd frontend && npm run serve",
    "start:electron": "nodemon electron/main.js",
    "start:dev": "npm-run-all --parallel start:backend start:frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.3",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "electron": "^28.1.4"
  }
}
